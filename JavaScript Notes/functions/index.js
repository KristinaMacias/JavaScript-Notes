//functions

/*
//define a function
 function catGreet () {
     console.log ('Hey cat! You are a cool animal!');
     console.log ('meow');
 }

 //call, invoke, run, execute a function
 catGreet();

 function specialGreeter (name) {
     console.log ('Hey, ' + name + '! You have a cool attitude.')
 }

 specialGreeter('Sader');

 function adder (a, b, c, d) {
     console.log (a + b + c + d);
 }
 
 adder(2, 3, 4, 5);

 function printerArray(mango) {
     for (var i = 0; i < mango.length; i++) {
         console.log (mango[i]);
     }
 }

 printerArray(['cat', 'kangaroo', 'dog', 'spider', 'raccoon', 'joe']);

 //function with return statements

 */

 function adder(num1, num2) {
     return num1 + num2;
 }
console.log(adder(2, 6));

// you want return inside functions instead of console.log

